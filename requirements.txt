torch>=2.0.0
onnxruntime>=1.16.0
psutil>=5.9.0
pywin32; sys_platform == 'win32' or sys_platform == 'win64'  # Для Windows-хуков
linux-api-header; sys_platform == 'linux'  # Для Linux-перехвата
pyyaml>=6.0.0
pytest>=7.0.0  # Для тестов
fastapi>=0.109.0  # Для API‑интеграций
uvicorn>=0.29.0  # Для веб‑интерфейса
